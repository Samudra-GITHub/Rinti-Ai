{
  "buildCommand": "npm run build",
  "installCommand": "npm ci && cd backend && pip install -r requirements.txt",
  "framework": "nextjs",
  "functions": {
    "api/index.py": {
      "runtime": "python3.12",
      "maxDuration": 60
    }
  }
}
